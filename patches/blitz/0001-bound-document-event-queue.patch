diff --git a/crates/blitz-dom/src/document.rs b/crates/blitz-dom/src/document.rs
index 4e99af6..0d9a509 100644
--- a/crates/blitz-dom/src/document.rs
+++ b/crates/blitz-dom/src/document.rs
@@ -38,7 +38,7 @@ use std::ops::{Deref, DerefMut};
 use std::rc::Rc;
 use std::str::FromStr;
 use std::sync::atomic::{AtomicUsize, Ordering};
-use std::sync::mpsc::{Receiver, Sender, channel};
+use std::sync::mpsc::{Receiver, SyncSender, sync_channel};
 use std::sync::{Arc, Mutex, MutexGuard, OnceLock, RwLockReadGuard, RwLockWriteGuard};
 use std::task::{Context as TaskContext, Waker};
 use style::Atom;
@@ -70,6 +70,11 @@ use thin_vec::ThinVec;
 use url::Url;
 use web_time::Instant;
 
+// Resource completions may retain decoded image buffers until the document is
+// polled. Keep the queue bounded so a hidden or stalled view has a fixed
+// memory ceiling; producers apply backpressure until the UI drains it.
+const DOCUMENT_EVENT_QUEUE_CAPACITY: usize = 16;
+
 #[cfg(feature = "parallel-construct")]
 use thread_local::ThreadLocal;
 
@@ -214,7 +219,7 @@ pub struct BaseDocument {
     pub(crate) subdocument_depth: usize,
 
     // Events
-    pub(crate) tx: Sender<DocumentEvent>,
+    pub(crate) tx: SyncSender<DocumentEvent>,
     // rx will always be Some, except temporarily while processing events
     pub(crate) rx: Option<Receiver<DocumentEvent>>,
 
@@ -447,7 +452,7 @@ impl BaseDocument {
             .html_parser_provider
             .unwrap_or_else(|| Arc::new(DummyHtmlParserProvider));
 
-        let (tx, rx) = channel();
+        let (tx, rx) = sync_channel(DOCUMENT_EVENT_QUEUE_CAPACITY);
 
         let mut doc = Self {
             id,
diff --git a/crates/blitz-dom/src/iframe.rs b/crates/blitz-dom/src/iframe.rs
index 801366d..6fa0f02 100644
--- a/crates/blitz-dom/src/iframe.rs
+++ b/crates/blitz-dom/src/iframe.rs
@@ -1,7 +1,7 @@
 //! Loading of `<iframe>` elements into sub-documents.
 
 use std::sync::Arc;
-use std::sync::mpsc::Sender;
+use std::sync::mpsc::SyncSender;
 
 use blitz_traits::navigation::{NavigationOptions, NavigationProvider};
 use blitz_traits::net::{AbortController, AbortSignal, Url};
@@ -31,7 +31,7 @@ pub(crate) struct IframeLoad {
 /// document, which reloads the iframe's sub-document with the new URL.
 struct IframeNavigationProvider {
     node_id: NodeId,
-    tx: Sender<DocumentEvent>,
+    tx: SyncSender<DocumentEvent>,
     shell_provider: Arc<dyn ShellProvider>,
 }
 
diff --git a/crates/blitz-dom/src/net.rs b/crates/blitz-dom/src/net.rs
index b36e2d7..d5c4f67 100644
--- a/crates/blitz-dom/src/net.rs
+++ b/crates/blitz-dom/src/net.rs
@@ -3,7 +3,7 @@ use selectors::context::QuirksMode;
 use std::sync::atomic::Ordering as Ao;
 use std::{
     io::Cursor,
-    sync::{Arc, atomic::AtomicUsize, mpsc::Sender},
+    sync::{Arc, atomic::AtomicUsize, mpsc::SyncSender},
 };
 use style::{
     font_face::{FontFaceSourceFormat, FontFaceSourceFormatKeyword, FontStyleRange, Source},
@@ -70,14 +70,14 @@ pub(crate) struct ResourceHandler<T: Send + Sync + 'static> {
     doc_id: usize,
     request_id: usize,
     node_id: Option<NodeId>,
-    tx: Sender<DocumentEvent>,
+    tx: SyncSender<DocumentEvent>,
     shell_provider: Arc<dyn ShellProvider>,
     data: T,
 }
 
 impl<T: Send + Sync + 'static> ResourceHandler<T> {
     pub(crate) fn new(
-        tx: Sender<DocumentEvent>,
+        tx: SyncSender<DocumentEvent>,
         doc_id: usize,
         node_id: Option<NodeId>,
         shell_provider: Arc<dyn ShellProvider>,
@@ -95,7 +95,7 @@ impl<T: Send + Sync + 'static> ResourceHandler<T> {
     }
 
     pub(crate) fn boxed(
-        tx: Sender<DocumentEvent>,
+        tx: SyncSender<DocumentEvent>,
         doc_id: usize,
         node_id: Option<NodeId>,
         shell_provider: Arc<dyn ShellProvider>,
@@ -174,7 +174,7 @@ impl NetHandler for ResourceHandler<StylesheetHandler> {
 
 #[derive(Clone)]
 pub(crate) struct StylesheetLoader {
-    pub(crate) tx: Sender<DocumentEvent>,
+    pub(crate) tx: SyncSender<DocumentEvent>,
     pub(crate) doc_id: usize,
     pub(crate) net_provider: Arc<dyn NetProvider>,
     pub(crate) shell_provider: Arc<dyn ShellProvider>,
@@ -366,7 +366,7 @@ impl FontFaceHandler {
 
 #[allow(clippy::too_many_arguments)]
 pub(crate) fn fetch_font_face(
-    tx: Sender<DocumentEvent>,
+    tx: SyncSender<DocumentEvent>,
     doc_id: usize,
     node_id: Option<NodeId>,
     sheet: &Stylesheet,
